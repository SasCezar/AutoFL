# @package _global_
defaults:
  - _self_
  - local: docker
  - taxonomy: gitranking
  - annotator: default
  - version_strategy: latest
  - dataloader: postgres
  - writer: postgres

hydra:
  output_subdir: null
  run:
    dir: .