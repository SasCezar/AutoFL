# @package _global_
defaults:
  - _self_
  - local: docker
  - taxonomy: gitranking
  - annotator: default
  - version_strategy: latest
  - dataloader: postgres
  - writer: postgres

package_annotation: True
project_annotation: True

hydra:
  output_subdir: null
  run:
    dir: .